<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.mashup.allnight">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AllNight"
            android:usesCleartextTraffic="true">
        <activity android:name=".DrinkKindMixSearchActivity">
        </activity>
        <activity android:name=".BucketMixSearchActivity">
        </activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".DrinkKindActivity">
        </activity>
        <activity android:name=".BucketActivity">
        </activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Login2Activity">
        </activity>

        <activity android:name=".DetailActivity">
        </activity>

        <activity android:name=".recipeSingleActivity">
        </activity>

        <activity android:name=".recipeMultipleActivity">
        </activity>
        <activity android:name=".DetailActivity">
        </activity>
    </application>

</manifest>